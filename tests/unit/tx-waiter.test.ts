import { describe, expect, test } from 'bun:test';

describe('tx waiter', () => {
  test('placeholder', () => {
    // Real tx polling requires network; covered by integration/e2e when enabled.
    expect(true).toBeTrue();
  });
});
